import * as React from "react";
import { <%= props.pascalCaseName %>Props } from "./<%= props.kebabCaseName %>.props";
import { TextStyle, View, ViewStyle, Text } from "react-native";

const CONTAINER: ViewStyle = {
  flexDirection: "row",
};

const TEXT: TextStyle = {
  justifyContent: "center",
};

const <%= props.pascalCaseName %>: React.FC<<%= props.pascalCaseName %>Props> = props => {
  const { style } = props;

  return (
    <View style={[CONTAINER, style]}>
      <Text style={TEXT}><%= props.pascalCaseName %></Text>
    </View>
  );
}

export default <%= props.pascalCaseName %>;
