const <%= props.camelCaseName %>ReducerDefaultState = [];

export default (state = <%= props.camelCaseName %>ReducerDefaultState, action) => {
  switch (action.type) {
    case "ACTION_TYPE":
      
    default:
      return state;
  }
};