import { <%= props.camelCaseName %> } from "../../types";
import { action } from "../../actions/<%= props.camelCaseName %>/types";

const <%= props.camelCaseName %>ReducerDefaultState: Readonly<<%= props.camelCaseName %>[]> = [];

export default (state = <%= props.camelCaseName %>ReducerDefaultState, action : Action) => {
  switch (action.type) {
    case "ACTION_TYPE":
      
    default:`
      return state;
  }
};