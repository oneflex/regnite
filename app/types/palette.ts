import { Green, Neutral, Primary, Red, Text, Yellow } from "./colors";

export interface Palette {
  primary: Primary;
  text: Text;
  neutral: Neutral;
  red: Red;
  yellow: Yellow;
  green: Green;
}
